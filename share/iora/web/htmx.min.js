/*! htmx vendored by iora — pinned version marker: htmx 1.9.12 (placeholder)
 * SPDX-License-Identifier: BSD-2-Clause (htmx upstream)
 * iora self-hosts this file (LD-10, no CDN). Replace with the real minified
 * htmx.min.js on an iora release bump; this marker keeps the pinned version
 * auditable. iora_embed_assets() folds this into a consumer's embedded static
 * set keyed 'htmx.min.js' (served at /static/htmx.min.js) unless NO_VENDOR_HTMX.
 */
(function(){/* htmx placeholder body — replaced by the real minified bundle on release */})();
